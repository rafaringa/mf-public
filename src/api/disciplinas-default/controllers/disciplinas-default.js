'use strict';

/**
 * disciplinas-default controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::disciplinas-default.disciplinas-default');
